---
import '../styles/global.css';
import BrandMark from './BrandMark.astro';
import TermTooltipScript from './TermTooltipScript.astro';
import { SITE } from '../lib/site';
import NetworkStrip from './NetworkStrip.astro';
import Analytics from './Analytics.astro';

interface Props {
  title: string;
  description?: string;
  type?: 'website' | 'article';
  datePublished?: string;
  dateModified?: string;
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
}
const {
  title,
  description = SITE.description,
  type = 'website',
  datePublished,
  dateModified,
  jsonLd,
} = Astro.props;

const fullTitle = title.includes(SITE.name) ? title : `${title} · ${SITE.name}`;
const canonical = new URL(Astro.url.pathname, SITE.domain).toString();
const orgLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: SITE.name,
  alternateName: SITE.nameEn,
  url: SITE.domain,
  description: SITE.description,
  inLanguage: 'ko-KR',
  publisher: {
    '@type': 'Organization',
    '@id': 'https://optiwork.co.kr/#organization',
    name: SITE.parent.name,
    url: SITE.parent.url,
  },
};
const graph = Array.isArray(jsonLd) ? jsonLd : jsonLd ? [jsonLd] : [];
const payload = [orgLd, ...graph];
---
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <meta name="naver-site-verification" content="9bf07dca926d48d6d104d8db1a76b5cf8cb6eb6a" />
<meta name="theme-color" content="#0B1220" />
    <link rel="canonical" href={canonical} />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="alternate" type="application/rss+xml" title={`${SITE.name} RSS`} href="/rss.xml" />
    <meta name="robots" content="index,follow,max-image-preview:large" />
    <meta name="author" content={SITE.name} />
    <meta property="og:site_name" content={SITE.name} />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content={type} />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content="ko_KR" />
    <meta property="og:image" content={new URL(SITE.ogImage, SITE.domain).toString()} />
    <meta name="twitter:image" content={new URL(SITE.ogImage, SITE.domain).toString()} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    {datePublished && <meta property="article:published_time" content={datePublished} />}
    {dateModified && <meta property="article:modified_time" content={dateModified} />}
    <script type="application/ld+json" set:html={JSON.stringify(payload)} />
    <Analytics />
  </head>
  <body>
    <a class="skip" href="#main">본문으로 건너뛰기</a>
    <div class="shell">
      <header class="topbar">
        <a class="brand" href="/">
          <span class="brand-mark"><BrandMark /></span>
          <span class="brand-text">
            <strong>{SITE.name}</strong>
            <span>{SITE.nameEn} · by Optiwork</span>
          </span>
        </a>
        <div class="topbar-actions">
          <nav class="nav" aria-label="주요 메뉴">
            <a href="/">브리핑</a>
            <a href="/about/">소개</a>
            <a href="/faq/">FAQ</a>
            <a href="/rss.xml">RSS</a>
          </nav>
          <a class="nav-telegram" href={SITE.telegram.url} target="_blank" rel="noopener noreferrer">
            {SITE.telegram.label}
          </a>
        </div>
      </header>
      <main id="main">
        <slot />
      </main>
      <NetworkStrip />
      <footer class="footer">
        <div><strong>{SITE.name}</strong> — 국내·해외 AI 커뮤니티 하루 동향</div>
        <div class="footer-telegram">
          <a class="btn-telegram" href={SITE.telegram.url} target="_blank" rel="noopener noreferrer">
            텔레그램에서 매일 받기 · {SITE.telegram.handle}
          </a>
        </div>
        <div>본문 속 점선 용어는 클릭하면 쉬운 설명이 나옵니다. · <a href={SITE.parent.url}>Optiwork</a></div>
        <div>© {new Date().getFullYear()} Optiwork · <a href="https://optiwork.co.kr">optiwork.co.kr</a></div>
      </footer>
    </div>
    <TermTooltipScript />
  </body>
</html>
